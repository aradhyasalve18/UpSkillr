// courseRoutes.js — Owner: Developer 2 (Course & Lesson Module Lead)
// GET /api/courses, GET /api/courses/:slug, POST /api/courses (instructor only),
// PUT /api/courses/:id, PATCH /api/courses/:id/publish.
// TODO: wire to courseController + authMiddleware.authorize('instructor').
