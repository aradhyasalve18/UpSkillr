// server.js — Owner: Developer 1 (Backend Core & Auth Lead)
// Express app entry point.
// TODO:
//   1. Load env vars (dotenv), call config/db.js connectDB()
//   2. Mount routes: /api/auth, /api/courses, /api/lessons,
//      /api/enrollments, /api/admin
//   3. Register middleware/errorMiddleware.js (notFound + errorHandler) last
//   4. app.listen(process.env.PORT || 5000)
