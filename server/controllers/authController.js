// authController.js — Owner: Developer 1 (Backend Core & Auth Lead)
// Handles register/login/me for /api/auth (FR-01).
// TODO: register (hash password, create User), login (verify + sign JWT),
// me (return req.user from authMiddleware).
