// Enrollment.js — Owner: Developer 3 (Enrollment & Progress Lead)
// Mongoose schema linking a learner to a course, with progress state.
// Fields to define: learnerId (ref User), courseId (ref Course),
// completedLessonIds[], enrolledAt, completedAt, rating, feedback.
// TODO: define schema, add a virtual/method for percent-complete.
