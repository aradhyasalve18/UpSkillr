// User.js — Owner: Developer 1 (Backend Core & Auth Lead)
// Mongoose schema for learners, instructors, and admins.
// Fields to define: name, email (unique), passwordHash, role
// ('learner' | 'instructor' | 'admin'), createdAt.
// TODO: define schema, pre-save bcrypt hook, export model.
